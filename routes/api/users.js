const express = require("express");
const router = express.Router();

// this will respond to anything sent to /api/users - you can see the routes defined fully in the server
router.get("/test");
